// Package ddl renders DDL from the normalized model.
//
// It belongs to the core layer: it must never import UI or
// framework packages. See docs of the architecture for the dependency rule.
package ddl

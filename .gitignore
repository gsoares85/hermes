.idea
docs
CLAUDE.md
.claude

# Build output
/bin/
*.exe
# The pipeline builds the headless binary once more with cgo switched off, to
# prove it still compiles without a C toolchain. Run locally, it leaves this.
/hermes-cli-static
# Task runner fingerprint cache
.task/
*.dll
*.so
*.dylib

# Test and coverage output
coverage.out
coverage.html

# Frontend
frontend/node_modules/
frontend/dist/*
!frontend/dist/.gitkeep

# Local environment
.env
.env.local
